import React from 'react'
import ReactDOM from 'react-dom/client'
import app from './app.tsx'
import './index.css'


ReactDOM.createRoot(document.get_element_by_id('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,)
)